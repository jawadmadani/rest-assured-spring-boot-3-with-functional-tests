
server:
  port : 8080
  servlet:
    context-path: "/uk"

bank-holidays:
  url: "http://localhost:${wiremock.server.port}/bank-holidays.json"

ip-information:
  url: "http://localhost:${wiremock.server.port}/ipinfo.io/json"
